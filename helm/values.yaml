replicaCount: 1

image:
  repository: nazyvaevdocker/juice-shop
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: ""
  annotations: {}
  hosts:
    - host: juice-shop.local
      paths:
        - path: /
    - host: 192.168.56.102.nip.io
      paths:
       - path: /
          pathType: Prefix
  tls: []
