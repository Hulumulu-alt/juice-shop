replicaCount: 1

image:
  repository: nazyvaevdocker/juice-shop
  tag: 4
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: ""
  annotations: {}
  hosts:
    - host: juice-shop.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
